<aside id="sidebar-wrapper">

    <div class="sidebar-brand">
        <h2>Escolão do Mutão</h2>
    </div>

    <ul class="sidebar-nav">
        <li class="<% if (page_name === 'overview') { %>
            dropdown active
            <%} else { %>
            dropdown
            <% } %>">
            <a class="ddbtn" href="/admin/dashboard"><i class="fa fa-home"></i>Visão Geral</a>
        </li>
        <li class="<% if (page_name === 'profile') { %>active<% } %>">
            <a href="/admin/profile"><i class="fa fa-id-card"></i>Perfil</a>
        </li>

        <li class="<% if (page_name === 'staff') { %>
            dropdown active
            <%} else { %>
            dropdown
            <% } %>">
            <a class="ddbtn" href="#"><i class="fa fa-chalkboard-teacher"></i>Funcionários</a>
            <div class="dropdown-content">
                <a href="/admin/addStaff">Adicionar</a>
                <a href="/admin/getStaff">Buscar</a>
            </div>
        </li>


        <li class="<% if (page_name === 'students') { %>
            dropdown active
            <%} else { %>
            dropdown
            <% } %>">
            <a class="ddbtn" href="#"><i class="fa fa-book-reader"></i>Estudantes</a>
            <div class="dropdown-content">
                <a href="/admin/addStudent">Adicionar</a>
                <a href="/admin/getStudent">Buscar</a>
            </div>
        </li>

        <li class="<% if (page_name === 'courses') { %>
            dropdown active
            <%} else { %>
            dropdown
            <% } %>">
            <a class="ddbtn" href="#"><i class="fa fa-award"></i>Cursos</a>
            <div class="dropdown-content">
                <a href="/admin/addCourse">Adicionar</a>
                <a href="/admin/getCourse">Buscar</a>
            </div>
        </li>

        <li class="<% if (page_name === 'classes') { %>
            dropdown active
            <%} else { %>
            dropdown
            <% } %>">
            <a class="ddbtn" href="#"><i class="fa fa-chalkboard"></i>Turmas</a>
            <div class="dropdown-content">
                <a href="/admin/addClass">Adicionar</a>
                <a href="/admin/getClass">Buscar</a>
            </div>
        </li>

        <li class="<% if (page_name === 'depts') { %>
            dropdown active
            <%} else { %>
            dropdown
            <% } %>">
            <a class="ddbtn" href="#"><i class="fa fa-school"></i>Departamentos</a>
            <div class="dropdown-content">
                <a href="/admin/addDept">Adicionar</a>
                <a href="/admin/getDept">Buscar</a>
            </div>
        </li>



        <hr>
        <li class="<% if (page_name === 'settings') { %>
            dropup active
            <%} else { %>
                dropup
            <% } %>">
            <a class="ddbtn" href="#"><i class="fa fa-cog"></i>Configurações</a>
            <div class="dropup-content">
                <a href="/admin/info_settings">Conta</a>
                <a href="/admin/password_settings">Senha</a>
            </div>
        </li>
        <li>
            <a href="/admin/logout"><i class="fa fa-sign-out-alt"></i>Sair</a>
        </li>
    </ul>
</aside>

<div id="navbar-wrapper" style="position: fixed;">
    <nav class="navbar navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
                <a href="#" class="navbar-brand" id="sidebar-toggle"><i class="fa fa-bars"></i></a>
            </div>
        </div>
    </nav>
</div>